import type { Problem } from '../types'

// L6 "자동화" — 배열 · read · 스크립트(M3: $(( ))/(( )), 파라미터 확장, "$@",
// here-document, read -a 등). Task 2/3에서 문제 10개를 채운다. 지금은 빈
// 배열이라 LevelSelect의 total === 0 가드에 의해 COMING SOON으로 표시된다.
export const l6: Problem[] = []
